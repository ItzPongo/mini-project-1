<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog Posts</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Blog Posts</h1>

  <h2>Posts</h2>
  <ul>
    <% posts.forEach(post => { %>
      <li>
        <strong><%= post.title %></strong> by <%= post.author %> on <%= post.timestamp %><br>
        <a href="/post/<%= post.id %>" class="button-like">View</a>
		<a href="/edit/<%= post.id %>" class="button-like">Edit</a>
        <form action="/delete/<%= post.id %>" method="POST" class="inline">
		  <button type="submit">Delete</button>
		</form>
      </li>
    <% }) %>
  </ul>
  
  <a href="/new" class="button-like">Create New Post</a>
</body>
</html>